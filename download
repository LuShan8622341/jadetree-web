import { motion } from 'motion/react';
import { BookOpen, Settings, Lightbulb, School } from 'lucide-react';

export default function Education() {
  const pillars = [
    {
      icon: BookOpen,
      title: "1+1+X 创新人才培养",
      items: ["无人机装调检修工认证", "工业机器人应用实训", "人工智能专项课程", "CAAC 执照与视距内/超视距驾驶"]
    },
    {
      icon: Settings,
      title: "产教融合实训基地",
      items: ["校企共建现代产业学院", "真实产业项目进校园", "“学徒制”深度教学模式", "教师“双师型”队伍库构建"]
    },
    {
      icon: Lightbulb,
      title: "创孵中心与转化",
      items: ["无人机/3C 售后创业支持", "低成本、低风险实践平台", "核心技术成果小试/中试", "毕业生入职集团合作企业"]
    }
  ];

  return (
    <div className="bg-black pt-40 pb-20 min-h-screen">
      <div className="container mx-auto px-8">
        <motion.div
           initial={{ opacity: 0, scale: 0.95 }}
           animate={{ opacity: 1, scale: 1 }}
           className="text-center mb-32"
        >
          <span className="text-brand-400 uppercase tracking-[0.5em] text-sm mb-6 block">Industry Academy</span>
          <h1 className="text-6xl lg:text-9xl font-black uppercase tracking-tighter mb-10">
            教育<span className="text-brand-400">板块</span>
          </h1>
          <p className="text-xl lg:text-2xl font-light opacity-50 max-w-3xl mx-auto leading-relaxed">
            产教融合的领先者。我们通过专业的培训体系与创业支撑，为产业输送顶尖的数字化人才。
          </p>
        </motion.div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12 mb-32">
          {pillars.map((pillar, i) => (
            <motion.div
              key={i}
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.1 }}
              className="bg-zinc-900/50 p-12 rounded-[3rem] border border-white/5"
            >
              <pillar.icon className="text-brand-400 mb-10" size={40} strokeWidth={1.5} />
              <h3 className="text-3xl font-bold mb-8">{pillar.title}</h3>
              <ul className="space-y-4">
                {pillar.items.map((item, j) => (
                  <li key={j} className="text-lg font-light opacity-50 flex items-center gap-3">
                    <div className="w-1.5 h-1.5 rounded-full bg-brand-400" />
                    {item}
                  </li>
                ))}
              </ul>
            </motion.div>
          ))}
        </div>

        <motion.div
           initial={{ opacity: 0 }}
           whileInView={{ opacity: 1 }}
           viewport={{ once: true }}
           className="bg-zinc-900 rounded-[4rem] p-12 md:p-20 flex flex-col md:flex-row items-center justify-between gap-12"
        >
           <div className="max-w-xl">
              <div className="flex items-center gap-4 text-brand-400 mb-6 font-bold tracking-[0.3em] uppercase">
                <School /> Academy Partners
              </div>
              <h2 className="text-4xl lg:text-5xl font-black mb-8">联接全球院校<br />共建智能未来</h2>
              <p className="text-xl font-light opacity-40 leading-relaxed">
                欢迎职业院校、综合大学及社会教育机构入驻，共同开拓产教融合业务渠道。
              </p>
           </div>
           
           <div className="grid grid-cols-1 sm:grid-cols-2 gap-6 w-full md:w-auto">
              <div className="p-8 border border-white/5 rounded-3xl bg-black/40 text-center">
                 <p className="text-4xl font-bold mb-2">30+</p>
                 <p className="text-xs uppercase tracking-widest opacity-40">合作院校</p>
              </div>
              <div className="p-8 border border-white/5 rounded-3xl bg-black/40 text-center">
                 <p className="text-4xl font-bold mb-2">100k</p>
                 <p className="text-xs uppercase tracking-widest opacity-40">辐射学员</p>
              </div>
           </div>
        </motion.div>
      </div>
    </div>
  );
}
